# Serial API

序列埠速率為 230400。每個指令以換行結束，大小寫不敏感。API 只調整決策後的類別偏移，不會改寫指紋資料庫或任何樹節點。

## 讀取設定

```text
API GET
```

回傳輸出週期、模型版本、取樣率、資料窗大小與四個使用者偏移值。

## 立即讀取狀態

```text
API STATUS
```

立即回傳一筆 `activity_state`；不必等待下一個固定 5 秒週期。

## 查看模型比對結果

```text
API MODEL
```

回傳實際送入決策樹的六個特徵、樹的數量、節點數，以及四個狀態的原始投票比例。這個回應可用來確認 ESP32 確實正在比對內建資料庫。

六個特徵依序是：

1. 加速度向量平均值
2. 加速度向量標準差
3. 加速度向量變異數
4. 角速度向量平均值
5. 角速度向量標準差
6. 角速度向量變異數

## 調整活動靈敏度

```text
API BIAS <LEVEL> <VALUE>
```

- `LEVEL`：`0` 靜止、`1` 輕度、`2` 中度、`3` 高度。
- `VALUE`：`-3` 至 `3`。
- 正值會提高該狀態被選中的機會；負值會降低。
- 四個值預設全部為 `0`。零代表使用程式內既有的基礎判定值。

範例：

```text
API BIAS 2 1
```

## 感測器重新連線

```text
API RESTART
```

清除舊資料窗、執行 I2C bus clear 並重新初始化 MPU6050，不會重新啟動整顆 ESP32。

